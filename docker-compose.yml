services:
  claude-code:
    image: taegost/claude-code-env:latest
    container_name: claude-code
    restart: unless-stopped
    env_file: .env
    volumes:
      - ~/.claude:/home/claude/.claude
      - ${WORKSPACE_PATH}:/workspace
